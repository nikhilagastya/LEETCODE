class Solution:
    def buildArray(self, target: List[int], n: int) -> List[str]:
        a=[]
        for i in range(1,target[-1 ]+1):
            if i in target:
                a.append('Push')    
            else:
                a.append('Push')
                a.append('Pop')
        return(a)
