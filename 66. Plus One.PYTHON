class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        if digits==[0,0]:
         return [0,1]
        else:
         return [int(i) for i in str(int("".join([str(i) for i in digits])) + 1)]
